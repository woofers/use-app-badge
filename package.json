{
  "name": "use-app-badge",
  "private": false,
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "dev": "turbo run dev",
    "build-site": "turbo run build-site",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write \"packages/**/{src,tests}/**/*.{jsx,js,tsx,ts}\""
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "workspaces": ["packages/*"],
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^1.13.2"
  },
  "overrides": {
    "use-app-badge": "~0.2.0",
    "standardized-app-badge": "~0.2.0"
  }
}